//constant variables that should be used across the project, only the important ones, responsible for defining the app itself

export const API_URL = `https://forkify-api.herokuapp.com/api/v2/recipes`;
export const TIMEOUT_SEC = 10;
